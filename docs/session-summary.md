# Decrypt — Session Handoff Summary

> Paste a pointer to this file in a new chat: **"Read `docs/session-summary.md` and continue."**

## Where things stand (as of 2026-06-10, second session)

- **Branch:** `harden-and-creator-acclaim` (branched off `main`).
- **Committed this session (on top of `fdf2334`):**
  - `d5531bd` — Tier-fitting engine + board cache (Phase 1)
  - `1a620b6` — Player creation flow: live tier availability, zero dead ends (Phase 2)
  - `bb996d4` — Mod injection publishes exactly the reviewed board (Phase 3)
  - `13f06bd` — Economy trim: acclaim-only creator payout, endless taper, milestone cuts (Phase 4)
  - `a59f1f5` — Copy sweep + jargon guard test (Phase 5)
  - `8acb53f` — Playtest follow-ups: approval-gate fix for fitted boards, advanced mode hidden, mod "Edit Challenge" menu action
  - `1b81c55` — Acclaim progress moved from My Ciphers cards to the creator quest (Quests → Milestones)
  - `271255b` — Vote bar placement fix, quest ladders (creator 1/5/10/25 + new "audience" likes group), achieved-by-% stats, Leaderboard/Stats/Quests UI polish
  - `c9514ad` — Quest card cleanup ("Not yet" removed; acclaim ⓘ popover replaces the closest-challenge card), flair renames (creator: Hit Maker/Chart Topper/Cult Classic/Hall of Fame; likes: Thumbs Up/Applause/Standing O/Encore), Stats collapsed to two tabs: one curated Overall set + an upgraded Flair Locker showing locked flairs with their unlock quests. StatsTab is now 'overall' | 'flairs'.
  - `3de36ee` — Info popover is a fixed viewport-clamped sheet (was sliding off-screen on phones); unlocked flairs lead the locker grid; standalone loading uses a solid `#0b1416` base (`.loading-screen-solid`, boot.css matched — backdrop-covered Suspense fallbacks stay transparent); result-screen voting is a compact vertical rail on the quote panel's left edge with counts inside the buttons (label removed).
  - `f667d02` — Vote buttons moved into normal flow BELOW the quote panel (the left rail obstructed the challenge line): centered horizontal pills with counts inside, top margin sized to clear the out-of-flow rating pill, so no overlap at any width. Gemini prompt hardened: authenticity as top-priority prose rules (real published lines, verbatim, complete self-contained thoughts, no fabrication/paraphrase), world sources welcome via established published English translations only, attribution format `CHARACTER - WORK` (dash, no commas, ≤28 chars), per-batch variety across works/cultures.
  - `ac01fad` — Pixel-art thumb sprites replace the vote-button emojis: `scripts/generate_pixel_ui_icons.py` gained `thumb_icons()` (24×24, game palette, thumbs-down = vertical flip) → `public/ui_thumb_up.png`/`ui_thumb_down.png`, registered as `thumbUp`/`thumbDown` in the UiSprite registry. Solver-heavy tests got explicit 30s timeouts (work is expansion-bounded; wall time scales with load since the difficulty estimate stopped clipping at 25ms).
  - **2026-06-12 batch — hearts UX, theme setting, edge fixes:**
    - Out-of-hearts dialog (`HeartPurchaseDialog`): Cancel replaced by a **Home** button (`heart-purchase-home`, closes + `openHome()`); new hearts indicator with a **live 1s countdown** to the next heart (`heart-dialog-countdown`, self-ticking via `getHeartState`); if a heart regenerates while the dialog is open a **"Keep playing"** button (`heart-purchase-resume`) appears so the player isn't forced through Home. The daily-limit "resets in" note now ticks too.
    - `useHeaderClock` enabled-condition extended: ticks during active challenge play OR whenever a heart countdown/infinite-hearts timer is pending (`heartClockPending` in GameApp). Fixes the frozen "+1 in mm:ss" on the hub header AND a real bug: `retry()` on a long-idle result screen used a stale clock and could open the heart dialog even though a heart had regenerated.
    - **Theme setting (Settings → Theme: Default | Minimal):** `themePreference` on the user profile (`shared/game.ts` `themePreferenceSchema`, state.ts hash round-trip sanitized so unknown values degrade to 'default' instead of tripping the profile-reset fallback, `profile.setThemePreference` mutation), mirrored to localStorage (`decrypt-theme-preference-v1`) for first-paint correctness. Minimal = flat solid background on every screen: GameApp drops `challenge-backdrop`/`home-backdrop`/`hub-live`/`home-live`/`result-backdrop`/`challenge-live` and adds `.theme-minimal`. **Light/dark follows the player's Reddit appearance via `prefers-color-scheme`** (per Devvit docs; no in-app toggle). CSS: `.theme-app.theme-minimal` block in index.css — light + dark variable sets, dark-mode button under-shadow flattening, punctuation/quote ink fixes, and result-screen adaptation (scoped `--app-overlay: transparent`, `text-white`→`var(--app-text)` and `border-white`→`var(--app-outline)` inside the outcome quote/time/rating pills, `--outcome-positive/negative` rating tones per scheme).
    - Edge fix: **duplicate flair "Hall of Fame"** — `milestone_creator_acclaim_25` collided with `milestone_daily_top_100` (locker showed dupes + React duplicate-key warning). Creator top flair renamed to **Living Legend**; `quests.test.ts` now pins catalog-wide flair uniqueness.
    - New tests: dialog countdown/home/resume, settings theme switch (mutation + root class + localStorage), `profile.setThemePreference` endpoint (valid + rejected input), flair uniqueness.
- **Gate status at last commit:** type-check clean, lint clean (0 warnings), **846 tests pass** (128 files), build + `check:preview-bundle` pass.
- **Not yet done:** PR not opened; no Devvit playtest of the new flows; branch not pushed.
- **Plan file:** `C:\Users\vritr\.claude\plans\misty-popping-toucan.md` (includes Devvit-docs verification notes).

## Project

`decrypttest` — Devvit Web cryptogram game on Reddit. Stack: tRPC v11, Hono, Redis, Reddit Gold payments. Constraints: Devvit Web only; `showToast`/`showForm`; `type` over `interface`; named exports; no TS casts unless unavoidable; menu/form endpoints mapped in devvit.json; preview.html React-free; TS strict + `exactOptionalPropertyTypes`. Devvit limits verified: server requests max 30s; Redis 40k cmd/s, 500MB; mod menu flows must complete within 10 minutes.

## What shipped this session (challenge-creation overhaul)

**The big idea:** difficulty tiers are now defined by solver-success bands, not text-profile gates. The engine FITS any line to a tier by searching over information-ranked reveals (letters completing A/I/THE/AND, suffixes, frequency) and obstructions, instead of rejecting lines ("too complex for easy" is gone). Boards are fitted once, cached (`decrypt:fit:*`, 45-min TTL, deterministic re-fit on miss), and the cached layout is persisted/applied verbatim — so the board a player previews or a mod reviews is byte-identical to the board that publishes.

- **Engine:** `src/server/core/tier-fitter.ts` (fitBoardToTier, adaptive reveal caps, honest impossibility: expert needs ≥14 unique letters/≥45 letters, hard ≥10), `board-fit-service.ts` (fitLineToTiers + Redis cache), `board-layout.ts` (apply layout to fresh base). `solverBandForTier` in solver-thresholds.ts (floors = legacy build gates).
- **Player flow:** `community.fitLine` endpoint; tier buttons show ✓/greyed-with-reason live while typing (650ms debounce); selection auto-bumps to the suggested tier; submit stores `fittedLayout` on the submission; approval applies it verbatim. Advanced mode: solver-derived advice ("Reveal the E in 'THERE' — E appears 6 times") and a "Fix it for me" button (`community.autoFixManualLayout`) that minimally repairs layouts and lists changes.
- **Mod flow:** step 1 = fitLineToTiers + duplicate check, review form bounded to feasible tiers (excluded tiers listed with reasons); step 2 = `publishFittedManualPuzzle` (admin.ts) — applies the cached layout, no adjustPuzzleDifficulty, no reseed. Offered tiers can no longer fail at publish.
- **Determinism war story (important):** three separate solver call sites used small wall-clock budgets (10–75ms), so CPU load changed which boards were built/judged fair. All fit-path solver calls now budget by branch expansions only (time cap raised to never bind): `choosePadlockObstruction` (via new `deterministicSolverBudget` flag on buildPuzzle), manual fairness checks, `buildDifficultyBreakdown`. Gold/padlock rngs seeded independently of loop history. This also fixed the historically flaky difficulty-adjustment test.
- **Economy:** approval now grants ONLY the Puzzle Maker flair (the 200-coin payout removed — acclaim quests are the only creator coins, as originally decided). Endless taper: clears 1–5/day full reward, 6–10 half, 11+ flat 10 coins (journaled counter, 48h TTL, friendly notice). Top milestones trimmed (daily_top_100 3000→1800 etc. — snapshot test pins the curve). New `src/shared/economy.ts` centralizes knobs; constants.ts re-exports. Powerup/heart/retry/bundle pricing untouched.
- **Copy:** plain & friendly register, fix-first, tiers always Easy/Medium/Hard/Expert (mod forms too). `copy-tone.test.ts` enforces a banned-jargon list (solver, fairness checker, hardness, validation, obstruction, budget, engine, buildability, warmup) over the reason-producing functions.
- **Playtest follow-ups (`8acb53f`):** approval no longer re-runs the legacy text-profile gate on submissions carrying a fitted/manual board (the "crypto hardness is outside..." rejection of fitter-approved boards is fixed; structural checks only + the board re-validates on apply). Advanced board mode is hidden behind `advancedBoardModeEnabled = false` in CommunityScreen (server support intact for legacy manual submissions; flip the flag to bring it back). The create-page teaser is removed. New mod **"Decrypt: Edit Challenge"** post menu action (daily + injected posts; community posts redirect to Request Changes): prefilled form, edits re-fit and replace in place (`applyChallengeEdit`/`prepareChallengeEdit` in admin.ts, `keyPostLevel` reverse index in puzzle-store); text/tier lock once the board has plays, author stays editable.

- **2026-06-11 batch (`1b81c55`, `271255b`):** acclaim progress lives under the creator quest in Quests → Milestones (My Ciphers cards keep only the 🏆 badge). Result-screen vote bar moved to the bottom band (was overlapping the rating pill). Creator ladder re-tiered 1/5/10/25 (Loved/Acclaimed/Beloved/Legendary Creator); new "audience" likes-received ladder 10/50/250/1000 (`lifetimeLikesReceived`, credited once per unique voter via grow-only `decrypt:community:liked_by:{levelId}` hash). Milestone cards show "Achieved by X% of players" (per-quest claim counters / known-player count via `quests.getStatus.milestoneClaimPercents`). Quest cards gained progress bars; Leaderboard got medals/YOU-highlight/zebra; Stats got Daily|Global|Endless tabs (Global MUST stay button index 1 — test-pinned), an At-a-Glance hero, and the endless split.

## Likely next steps (confirm with user)

1. **Devvit playtest on `decrypttest_dev`** — priority checks: type a previously-rejected line (e.g. a pangram) and confirm Easy is offered + previews instantly; submit→approve produces the previewed board; mod-inject two-step never fails on an offered tier; 6+ endless clears show the taper notice; result-screen vote bar sits at the bottom with no overlap on mobile; liking a challenge from a second account ticks the creator's likes quest exactly once; milestone cards show the % line after first claims.
2. Push branch + open PR.
3. Optional: move daily generation onto the fitter too (it still uses the legacy gate as a selection heuristic — intentional, unchanged).
