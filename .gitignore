node_modules
.yarn
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
webroot
dist
coverage
.vite
.cache

# TypeScript build artifacts (but keep hand-written .d.ts files)
**/*.js
**/*.js.map
!vite.config.js
!eslint.config.js
!**/*.d.ts

# Local editor / IDE files
.vscode/
.idea/
.kiro/

# Local environment and secrets
.env
.env.*
!.env.example

# Devvit local debug output
devvit_logs_capture*.txt

# Local workspace instructions / tooling
AGENTS.md
.agent.md
.claude/
.codex/
tmp/
progress.md
.agents/
skills-lock.json

# Inside tools/: keep the versioned project tsconfigs, ignore local-only helpers
# (Devvit MCP wrapper / docs cache used by Codex + Claude, plus the audit driver).
tools/devvit-mcp-cache/
tools/devvit-mcp-wrapper.mjs
tools/mcp-audit-driver.mjs

# Inside scripts/: keep documented project tools (lines:check, preview perf
# guard) versioned; everything else is a local one-off.
scripts/*
!scripts/check-lines.ts
!scripts/check-preview-bundle.mjs

# Local one-off debugging scripts (keep production/admin debug endpoints versioned)
scripts/debug-local/

# Local data dir (not imported by versioned code).
src/server/catalog/


.fallowrc.json